version: '3.7'

services:
  web:
    restart: always
    build: 
      context: ./cms_src
      dockerfile: Dockerfile
    volumes:
      - ./cms_src/:/usr/src/cms/
      - ./test_src/:/usr/src/test/
      - ./test_src/fixtures/:/usr/src/cms/website/fixtures/
      - ./test_src/media/:/usr/src/cms/media/
    ports:
      - 8000:8000
    environment:
      - DEBUG=0
      - SECRET_KEY='o@@xO=jrd=p0^17svmYpw!22-bnm3zz*%y(7=j+p*t%ei-4pi!'
      - SQL_ENGINE=django.db.backends.postgresql
      - SQL_DATABASE=personal_cms_db
      - SQL_USER=personal_cms
      - SQL_PASSWORD=N0tTh3D3favlTpAssw0rd
      - SQL_HOST=db
      - SQL_PORT=5432
    depends_on:
      - db
  db:
    image: postgres:10.5-alpine
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_PASSWORD=N0tTh3D3favlTpAssw0rd
      - POSTGRES_DB=personal_cms_db
      - POSTGRES_USER=personal_cms

volumes:
  postgres_data:
